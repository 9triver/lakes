# 512 逻辑 Patch 与可重建训练数据

> 2026-07-21：逻辑目录进一步改为用户独享，见 [Profile-owned Logical Patch ADR](docs/adr/0002-profile-owned-logical-patch-catalogs.md)。基础观测数据共享，Logical Patch manifest 和审核状态不共享。

人工审核只作用于固定的 `512 x 512` 逻辑 patch。逻辑网格按源影像左上角对齐，步长为 512，边缘不足部分补零并标记为无效；同一位置在不同训练样本或影像中分别审核。

逻辑目录按用户独立保存。实际训练数据由用户自己的 Logical Patch 选择和版本化 Dataset Config 生成，可随时重建。首批配置为 `resize256_v1` 和 `native512_v1`，默认使用 `min_valid_ratio=0.6`、`min_water_pixels=1`、`negative_ratio=0.25`。

区域详情提供默认关闭的 Patch 模式，按样本和影像显示原始底图及逻辑网格，支持在当前 Profile 中预览和批量排除/恢复。Profile 的成员或来源选择变化后，已经生成的训练数据变为过期，必须显式重建后才能训练。

迁移从现有 `training_samples.csv`、源影像和训练标注重建逻辑目录及两套实际数据。旧 `training_patches/ps256_st128` 保留但不再读取。

2026-07-16 最终迁移结果：5 个区域、16 个训练样本生成 573 个逻辑 patch；按最终人工审核状态，`resize256_v1` 生成 303 个实际 patch，`native512_v1` 生成 302 个实际 patch。全部预览和 NPZ 路径存在，NPZ 形状及 mask 值验证通过，5 个旧 `ps256_st128` 目录保持原样。
